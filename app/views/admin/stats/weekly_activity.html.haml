%html
  %head
    %title Sitewide activity
  %body
    %table
      %tr
        %th Weeks ago
        %th New users
        %th New top-level groups
        %th New discussions
        %th New comments
        %th New proposals
        %th New votes
      - @metrics.each do |m|
        %tr
          %td= m.fetch :weeks_ago
          %td= m.fetch :users
          %td= m.fetch :groups
          %td= m.fetch :discussions
          %td= m.fetch :comments
          %td= m.fetch :motions
          %td= m.fetch :votes
